export SOPS_AGE_KEY_FILE=$(expand_path ~/.config/sops/age/keys.txt)
export HOMELAB_PROJECT_DIR=$(expand_path .)
export HOMELAB_DEPLOY=dev
export ANSIBLE_PLAYBOOK_DIR=$(expand_path ansible/playbooks)


[[ -f .envrc.local ]] && source_env .envrc.local
